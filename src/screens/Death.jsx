import React, { useState } from 'react';
import { SafeAreaView, StyleSheet, Alert, Text } from 'react-native';

function Death() {
    return (
        <Text>Death Screen</Text>
    );
}

export default Death;